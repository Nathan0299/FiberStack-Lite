# == FIBER ETL SANDBOX ==
ETL_MODE=sandbox
ETL_POLL_INTERVAL=5

# DB
DB_HOST=${TS_HOST}
DB_PORT=${TS_PORT}
DB_USER=${TS_USER}
DB_PASSWORD=${TS_PASSWORD}
DB_NAME=${TS_DB}

# Logging
ETL_LOG_LEVEL=DEBUG
