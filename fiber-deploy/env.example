# FiberStack Environment Configuration
# Copy this file to .env and customize as needed

# Database Configuration
DB_HOST=timescaledb
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=fiberstack

# Grafana Configuration
GF_USER=admin
GF_PASS=admin

# Authorization Configuration (Required)
USER_CREDENTIALS=admin:admin
FEDERATION_SECRET=sandbox_secret

# Environment Mode (dev/staging/prod)
ENV=dev
