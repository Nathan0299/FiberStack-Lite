apiVersion: 1

providers:
  - name: 'FiberStack'
    orgId: 1
    folder: 'FiberStack'
    folderUid: 'fiberstack'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
